{
  "lr": 0.1,
  "optimizer": "adam",
  "max_epochs": 10,
  "early_stopping": true,
  "early_stopping_patience": 2,
  "early_stopping_delta": 0.05,
  "lr_scheduler": "warmup_cosine_annealing",
  "lr_scheduler_kwargs": {
    "base_lr": 0.001,
    "warmup_epochs": 2,
    "num_epochs": 10
  }
}